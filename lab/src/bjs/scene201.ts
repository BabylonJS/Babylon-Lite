// BJS reference for scene 201 — see scene200.ts for full comment.
// HPM has no BJS equivalent; this file is identical to scene200.ts and
// exists only so the bundle build infrastructure has a buildable target
// per scene-config entry.
export {};
import "./scene200";
