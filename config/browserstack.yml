# BrowserStack SDK Configuration
# Credentials: set BROWSERSTACK_USERNAME and BROWSERSTACK_ACCESS_KEY as env vars
# (via .env.local locally, or the BabylonJS-BrowserStack variable group in CI).
userName: BROWSERSTACK_USERNAME
accessKey: BROWSERSTACK_ACCESS_KEY

# Reporting
projectName: Babylon-Lite
buildName: ${BROWSERSTACK_BUILD_NAME}
buildIdentifier: "#${BUILD_NUMBER}"

# Target macOS for WebGPU support (Windows VMs lack GPU acceleration)
platforms:
    - os: OS X
      osVersion: Sonoma
      browserName: chrome
      browserVersion: latest

# BrowserStack runs are serialized so this pipeline uses one instance at a time.
parallelsPerPlatform: 1

# Local tunnel — only needed when tests hit a local dev server. When parity runs
# against a public static site (PARITY_BASE_URL set), the runner disables this.
# Driven by BROWSERSTACK_LOCAL (set by scripts/run-browserstack.ts); defaults to
# true so a bare SDK invocation still tunnels to localhost.
browserstackLocal: ${BROWSERSTACK_LOCAL}

framework: playwright

# Logging
debug: false
networkLogs: false
consoleLogs: errors
